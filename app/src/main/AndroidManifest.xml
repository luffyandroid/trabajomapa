<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fl.trabajomapa">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/appicon_cuadrado"
        android:label="@string/app_name"
        android:roundIcon="@drawable/appicon_circulo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AASplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".CAPublicarOfertaActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".BAMapaActivity"
            android:label="@string/title_activity_bamapa"
            android:theme="@style/AppTheme" />

        <activity android:name=".CCPublicadoActivity"
            android:screenOrientation="portrait" />

        <activity android:name=".CBPayPalActivity"
            android:screenOrientation="portrait" />

    </application>

</manifest>